@Library('lib-0') // Replace with the name of your global shared library

pipeline {
  agent any
  
  stages {
    stage('Build') {
      steps {
        // Build steps go here
      }
    }
    
    // Additional stages go here
  }
}
