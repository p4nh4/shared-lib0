

pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Add build steps here
            }
        }
        stage('Test') {
            steps {
                // Add test steps here
            }
        }
        // Add more stages as needed
    }
    post {
        always {
            // Add post-build actions here
        }
    }
}
